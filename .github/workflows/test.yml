name: Test Workflow

on:
 workflow_dispatch:
jobs:
  tests_manual_workflow:
    runs-on: self-hosted
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - run: |
        uname -m
        uname -a
